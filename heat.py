''' Heat equation exercise
creat by chinhsun,LU '''

import numpy as np
nx = 50;
ny = 50;
V = np.zeros((nx,ny))
Vnew = np.zeros((nx,ny))
'''for i in range(nx):
    for j in range(ny):
        a[i,j] = i*nx+j
print(range(nx))'''